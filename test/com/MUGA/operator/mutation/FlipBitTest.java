/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.MUGA.operator.mutation;

import com.evolutionary.operator.mutation.FlipBit;
import com.evolutionary.population.SimplePopulation;
import com.evolutionary.problem.BinaryString;
import com.evolutionary.problem.bits.OneMax;
import org.junit.Test;
import static org.junit.Assert.*;

/**
 *
 * @author zulu
 */
public class FlipBitTest {

    /**
     * Test of execute method, of class FlipBit.
     */
    @Test
    public void testExecute() {
//        FlipBit flip = new FlipBit(1.0); // probsbility 1
//        Population pop = new Population(1);
//        pop.createRandom(new OneMax(100));
//        boolean[] bits1 = ((BinaryString) pop.getIndividual(0).getClone()).getBitsGenome();
//        System.out.println(pop);
//        pop = flip.execute(pop);
//        System.out.println(pop);
//        boolean[] bits2 = ((BinaryString) pop.getIndividual(0).getClone()).getBitsGenome();
//        for (int i = 0; i < bits2.length; i++) {
//            assertTrue(bits1[i] != bits2[i]);
//        }
    }

    /**
     * Test of execute method, of class FlipBit.
     */
    @Test
    public void testExecute2() {
//        FlipBit flip = new FlipBit(0.0); // probsbility 0
//        Population pop = new Population(1);
//        pop.createRandom(new OneMax(100));
//        boolean[] bits1 = ((BinaryString) pop.getIndividual(0).getClone()).getBitsGenome();
//        System.out.println(pop);
//        pop = flip.execute(pop);
//        System.out.println(pop);
//        boolean[] bits2 = ((BinaryString) pop.getIndividual(0).getClone()).getBitsGenome();
//        for (int i = 0; i < bits2.length; i++) {
//            assertTrue(bits1[i] == bits2[i]);
//
//        }
    }

}
